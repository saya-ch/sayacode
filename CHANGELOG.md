# 更新日志

## 3.0.0（2026-09-24）

- 默认入口改为本机 WebUI：FastAPI 在 `127.0.0.1` 提供 API 与随 wheel 分发的 React、TypeScript、Vite 页面。`sayacode --no-open` 只打印访问地址，`--port` 可指定本机端口；未配置模型也能打开页面完成设置。
- 页面提供工作区与会话列表、对话与实时运行轨迹、LangGraph 待办、子 Agent 关系图、人工审批和独立 worktree 差异交付。后台运行由 Python 进程持有，浏览器连接只订阅状态。
- 新增多工作区宿主和带线程、任务、运行标识的 SSE 事件广播；会话消息、待办和审批仍从原生 checkpoint 读取，工作区、任务关系及记忆继续使用 Store。断线后可重新读取快照并补收近期事件。
- 模型、MCP、Skill、记忆、Jev 配置及项目诊断改为类型化 Web 操作。Web API 限定本机访问，使用启动令牌、会话 Cookie、来源检查与 CSRF 校验；工具仍以当前用户权限运行，不提供操作系统沙箱。
- 移除交互 TUI 和斜杠命令界面；保留 `-p` 单次任务、`--doctor`、`text/json/jsonl` 输出和原退出码约定。文档与源码入口改按 WebUI 组织。

## 2.2.0

- 新增长期记忆：LangMem 负责提出有来源的用户偏好和项目经验，LangGraph Store 保存跨会话记录，原始消息仍只在 checkpoint 中。自动学习默认关闭，由 `/memory enable` 显式开启。
- 将用户记忆和项目记忆分开；同一 Git 仓库的 worktree 共用项目身份，项目文件依据带读取时的摘要并在文件变化或复核期到期后失效。工具观察先作为候选，用户可确认、纠正、固定、复核或遗忘。
- 加入来源租约、并发提交检查、遗忘屏障和学习撤销；CLI 退出与重启可恢复已完成轮次，关闭学习后旧来源不会重新入队。独立的记忆模型配置保留端点指纹，避免未处理材料自动发送到变化后的服务。
- 主 Agent 在模型请求中以成对工具消息读取有限记忆数据，不把历史正文提升为系统指令。后台提取使用有界材料并过滤可识别凭据；额外整理失败不改变主任务退出码。
- `/memory` 增加方向键管理菜单、会话级设置、模型选择、本轮参考、来源预览和可配置无交互收尾；JSONL 输出脱敏的记忆更新、失败与延期事件。人工说明改由 `SAYACODE_HOME/instructions.md` 加载，旧文件式 `/memory init/append` 已移除。
- 从 2.1 升级且需要保留旧人工说明时，先检查旧 `memory.md` 或 `.sayacode/memory.md`，再手动复制到 `SAYACODE_HOME/instructions.md` 或项目的 `SAYACODE.md`；旧文件不会自动加载或迁移。
- 补充真实 LangMem 图链、SQLite 并发与崩溃恢复、真实 CLI 子进程和本地模拟模型端点验收。可选语义索引与真实供应商效果评测留待后续单独验证。

## 2.1.0

- 加入标准 Agent Skill：项目和用户目录发现、模型按需加载、会话内显式启用和受边界约束的资源读取；删除 Markdown 自定义命令展开与人格风格，保留回答语言设置。
- 终端斜杠命令改为可搜索的候选菜单，支持方向键选择；模型协议选择改为单选控件，保留非交互输出协议。
- 重构提示词工程：主 Agent 与 builder、planner、reviewer 使用稳定的系统角色契约，统一先取证、再改动、验证和交付的工作方法；子任务用户消息只保留真实任务与派发快照，项目约定使用明确边界。
- 统一交互终端视觉层级：启动上下文、设置向导、工具与任务状态、Jev 判定、本轮耗时和命令帮助使用同一主题；实时事件、命令结果和输入循环拆为独立模块，无头输出协议保持不变。
- 移除模型轮次和工具调用的产品层数量上限；真实模型或工具错误仍按原生失败传播，用户中止和进程退出仍通过 LangGraph 检查点与运行控制处理。
- builder 子 Agent 的 worktree 改为可选派发参数；默认隔离，显式 `--shared` 或 `use_worktree=false` 时直接使用父工作区，并在任务结果中显示实际工作区模式。
- 自动摘要阈值改为按可用输入上下文动态计算，默认目标约为窗口的 80%；上下文编辑保持关闭，避免未经确认清理模型可见的旧工具结果。
- 新增 Jev 自动审理信任档：通过 TypeSafe 官方异步 SDK 批量判断待审批工具调用，低风险自动批准、不确定调用转原生 HITL、高置信度越权调用返回结构化拒绝；审理失败安全转人工。只读档不再暴露 Shell。
- 后台任务升级为 continuable 子 Agent：父子保留独立 LangGraph 线程，通过持久 Inbox 双向通信；繁忙接收方在下一次模型调用前接收，空闲接收方自动继续，结算消息直接携带有界最终结果。
- 强化官方 Todo 工具的动态调整指引；子 Agent 派发携带父目标与 Todo 快照，并可提前报告改变父任务下一步的发现，父 Agent 按消息证据更新自己的计划。
- 按职责重组 `agent/`、`tools/`、`tasks/`、`extensions/` 和 `cli/`；应用入口只组装产品资源。移除旧根级大文件与重复的源码启动脚本，测试按领域分组，命令行安装入口统一为 `sayacode.cli.main:main`。
- 构建、计划和评审会话模式及分层权限规则，已替换为 `read_only`、`ask`、`jev` 和 `full` 四档信任等级。会话各自持有等级，新会话使用配置默认值。绝对文件路径和非只读档的 Shell 可以到达起始工作区之外；不承诺操作系统沙箱或敏感路径例外。
- 精简模型工具目录，只保留不同能力。Git 模型调用只做只读查询；Git 改动走 Shell。删除批量编辑、环境列表、命令安全预检，以及重复的工程、符号和文件工具。独立并发调用仍由原生 LangGraph ToolNode 完成。
- 构建器工作树用于组织交付，不提供安全隔离。子任务继承父任务信任，非 Git 构建器使用共享工作区。
- 厂商名模型路由已替换为显式协议配置，覆盖 OpenAI 对话补全、OpenAI 响应、Anthropic 消息、Gemini 内容生成和 Ollama 原生对话。配置新增服务端地址、模型编号、上下文长度和最大输出 token 数必填；可选接口密钥在终端保持隐藏。旧模型配置直接拒绝，不做迁移。
- 新增文本、工具和流式模型能力探测，以及协议层 HTTP 契约测试。无结构化输出支持的服务端默认关闭可选大模型工具筛选。
- 无密钥自定义服务端使用非秘密软件包凭证标记，避免把环境中的服务商接口密钥送往其他服务地址；显式密钥在需鉴权服务端继续有效。
- 刷新交互式命令行，含响应式启动视图、命令表格、Markdown 回答渲染、工具补全反馈、单项审批卡片和防提示干扰的后台通知。无界面格式保持不变。
- `/help` 补全并支持搜索，可用 `/help <command>`；新增 `/new` 开启新对话、`/models` 列出配置，以及交互式 `/model add` 设置向导。
- 多份依赖列表合并为一份跨平台 `uv.lock`；持续集成在 Windows 和 Ubuntu 上，对 Python 3.11 至 3.13 安装锁定依赖图。
- 明确 2.0 已定行为的文档，包括命名空间 MCP 工具、单项审批、任务恢复和受保护的工作树交付。

## 2.0.0

- 编程智能体重构到 LangChain `create_agent`，使用 LangGraph 检查点、流事件和人工介入中断。
- 新增异步终端，支持文本、JSON 和带编号 JSONL 单次输出。
- 新增文件、搜索、符号、Shell、Git、网页和工程工具，统一走产品权限检查。
- 新增 LangChain 中间件，负责上下文整理、总结、重试、限额、待办列表和可选工具筛选。未恢复的模型失败按失败运行上报；工具自动重试仅限面向读取的工具。
- 新增模型配置、持久会话、图检查点回退、审计事件和本地诊断。
- 新增受信任项目 MCP 服务端（含 `mcp__` 工具名）、命令钩子、Markdown 斜杠命令，以及后台构建、规划和评审任务。
- 新增单项审批决策、模型可见任务限时等待、暂停子任务显式审批，以及子任务恢复用配置快照。工作树应用拒绝运行中任务，清理保留未应用或已变动内容。
- 可安装包移到 `src/sayacode/`，质量门替换为只面向 2.0 的测试和源码检查。
